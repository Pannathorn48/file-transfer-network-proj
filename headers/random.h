/**
 * Returns true with the given probability
 * @param percent Probability as a percentage (0.0 to 100.0)
 * @return true if random hit occurs, false otherwise
 */
bool random_percent(double percent);